# ratatree 0.4.0 다이얼로그 완성 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 파일 열기 / 폴더 고르기 / 여러 파일 고르기 다이얼로그가 좁은 패널, 긴 경로, 큰 디렉토리, 빈 폴더에서 앱 개발자가 손대지 않아도 끝까지 되게 한다.

**Architecture:** 상태 로직은 `src/state.rs`의 `FilePickerState`에, 렌더링은 `src/widget.rs`의 `FilePicker`에 있다. `DirsOnly` 모드에서 목록 맨 앞에 현재 폴더를 뜻하는 `.` 엔트리를 넣고(`entry.path == current_dir`로 판별), 경로 바는 왼쪽을 잘라 뒤쪽을 보존하며, 상태 바는 `커서/전체`로 시작한다. 예제 `dialogs.rs`가 세 다이얼로그를 50x18 모달로 띄운다.

**Tech Stack:** Rust 1.88+, ratatui 0.30 (crossterm 재수출), tempfile(테스트), `TestBackend` 렌더 테스트. 새 의존성 없음.

**Spec:** `docs/superpowers/specs/2026-09-12-dialogs-design.md`

## Global Constraints

- MSRV `rust-version = "1.88"`. 새 의존성 추가 금지.
- `#![warn(missing_docs)]`가 켜져 있다. 새 `pub` 항목에는 rustdoc 필수. rustdoc은 시그니처 반복이 아니라 "왜"를 적는다.
- CI가 `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `RUSTDOCFLAGS="-D warnings" cargo doc`를 돌린다. 매 커밋 전에 `cargo fmt`를 돌린다.
- TDD. 테스트를 먼저 쓰고 실패를 확인한 뒤 구현한다.
- 문서와 주석에서 앰대쉬(—) 금지, 문장 중간 줄바꿈(하드 랩) 금지.
- 커밋 메시지 끝에 `Claude-Session: https://claude.ai/code/session_01NowgbVqobryP8Qh2mN62Vz` 한 줄.
- `.` 엔트리는 `PickerMode::DirsOnly`에서만 넣는다. `Both`는 빌더 기본값이라 넣지 않는다.
- 배포(`cargo publish`)와 push는 이 계획의 범위 밖. 마지막 태스크는 검증까지만 한다.

## 파일 구조

| 파일 | 역할 | 이번 변경 |
|---|---|---|
| `src/state.rs` | `FilePickerState`, `CommonState`, 빌더, 탐색/선택 로직, 단위 테스트 | `.` 엔트리 삽입과 판별, `confirm`/`enter_directory`/`set_expanded_current` 가드, 문서 |
| `src/widget.rs` | `FilePicker` 렌더링, 렌더 테스트 | 경로 바 왼쪽 잘라내기 함수, 상태 바 위치 표시 |
| `tests/integration.rs` | 키 입력 기준 end-to-end 테스트 | 폴더 고르기 흐름 1개 |
| `examples/dialogs.rs` | 새 예제 | 생성 |
| `Cargo.toml` | 메타데이터 | `[[example]] dialogs`, 버전 0.4.0 |
| `README.md`, `CHANGELOG.md` | 문서 | 목업, Features, 키 표, 예제 실행, 0.4.0 항목 |

기존 테스트 헬퍼 (그대로 씀):
- `src/state.rs` tests: `make_dir_with_files()` → `alpha.txt`, `beta.rs`, `subdir/` (정렬: `subdir`, `alpha.txt`, `beta.rs`). `make_tree_dir()` → `(TempDir, root)`이고 root 아래 `a_dir/nested/deep.txt`, `a_dir/inner.txt`, `b_dir/`, `top.txt` (정렬: `a_dir`, `b_dir`, `top.txt`). root는 `dunce::canonicalize`된 경로.
- `src/widget.rs` tests: `make_dir_with_files()` (같은 구성), `make_dir_with_n_files(n)` → `f00.txt`..., `row_text(&terminal, y) -> String` (행 텍스트, 오른쪽 공백 제거).
- `tests/integration.rs`: `setup_test_dir()` → `src/`, `tests/`, `Cargo.toml`, `README.md`, `.gitignore`(숨김). `key(KeyCode) -> Event`, `key_char(char) -> Event`.

---

### Task 1: `DirsOnly` 목록 맨 앞에 `.` 엔트리 넣기

**Files:**
- Modify: `src/state.rs` (`refresh_entries` 229-249행 부근, `build()`의 `let (entries, read_error)` 725행 부근, `PickerMode` 문서 22-33행, `CommonState::entries` 문서 76행 부근, 파일 끝 tests 모듈)

**Interfaces:**
- Produces: `fn current_dir_entry(current_dir: &Path) -> Entry` (state.rs 내부 자유 함수). Task 2가 `entry.path == self.common.current_dir` 판별에 기댄다.

- [ ] **Step 1: 실패하는 테스트 6개 추가**

`src/state.rs`의 `mod tests` 안, `fn builder_with_tree_view()` 테스트 앞에 추가:

```rust
    #[test]
    fn dirs_only_lists_the_current_directory_first() {
        let dir = make_dir_with_files();
        let state = FilePickerState::builder()
            .start_dir(dir.path())
            .mode(PickerMode::DirsOnly)
            .build();

        let first = &state.common.entries[0];
        assert_eq!(first.name, ".");
        assert_eq!(first.path, state.common.current_dir);
        assert_eq!(first.kind, EntryKind::Directory);
        assert_eq!(first.depth, 0);
        assert_eq!(
            state.common.entries.len(),
            4,
            "subdir, alpha.txt, beta.rs plus the dot entry"
        );
    }

    #[test]
    fn other_modes_do_not_list_the_current_directory() {
        let dir = make_dir_with_files();
        for mode in [PickerMode::FilesOnly, PickerMode::Both] {
            let state = FilePickerState::builder()
                .start_dir(dir.path())
                .mode(mode)
                .build();
            assert_ne!(state.common.entries[0].name, ".", "{mode:?}");
            assert_eq!(state.common.entries.len(), 3, "{mode:?}");
        }
    }

    #[test]
    fn dirs_only_lists_the_current_directory_even_when_empty() {
        let dir = TempDir::new().unwrap();
        let state = FilePickerState::builder()
            .start_dir(dir.path())
            .mode(PickerMode::DirsOnly)
            .build();

        assert_eq!(state.common.entries.len(), 1);
        assert_eq!(state.common.entries[0].name, ".");
    }

    #[test]
    fn dirs_only_has_no_current_directory_entry_when_the_read_fails() {
        let dir = TempDir::new().unwrap();
        let missing = dir.path().join("no-such-directory");
        let state = FilePickerState::builder()
            .start_dir(&missing)
            .mode(PickerMode::DirsOnly)
            .build();

        assert!(
            state.common.entries.is_empty(),
            "a directory that cannot be read must not be offered for picking"
        );
        assert!(state.common.read_error.is_some());
    }

    #[test]
    fn dirs_only_keeps_exactly_one_current_directory_entry_after_refresh() {
        let dir = make_dir_with_files();
        let mut state = FilePickerState::builder()
            .start_dir(dir.path())
            .mode(PickerMode::DirsOnly)
            .build();

        state.toggle_hidden(); // goes through refresh_entries
        state.refresh_entries();

        let dots = state.common.entries.iter().filter(|e| e.name == ".").count();
        assert_eq!(dots, 1);
        assert_eq!(state.common.entries[0].name, ".");
    }

    #[test]
    fn tree_view_lists_the_current_directory_once_at_the_root() {
        let (_tmp, root) = make_tree_dir();
        let mut state = FilePickerState::builder()
            .start_dir(&root)
            .mode(PickerMode::DirsOnly)
            .view(ViewMode::Tree)
            .build();
        assert_eq!(state.common.entries[0].name, ".");

        *state.view.cursor_mut() = 1; // a_dir
        state.expand_current();

        let dots: Vec<&Entry> = state.common.entries.iter().filter(|e| e.name == ".").collect();
        assert_eq!(dots.len(), 1, "expanded subdirectories must not get their own dot entry");
        assert_eq!(dots[0].depth, 0);
        assert!(state.common.entries.iter().any(|e| e.name == "inner.txt"), "a_dir was expanded");
    }
```

- [ ] **Step 2: 실패 확인**

Run: `cargo test --locked --lib "current_directory"`
Expected: `dirs_only_lists_the_current_directory_first`, `dirs_only_lists_the_current_directory_even_when_empty`, `dirs_only_keeps_exactly_one_current_directory_entry_after_refresh`, `tree_view_lists_the_current_directory_once_at_the_root` 4개 FAILED (`entries[0].name`이 `"subdir"`/`"a_dir"`이거나 빈 목록 인덱스 패닉). `other_modes_do_not_list_the_current_directory`와 `dirs_only_has_no_current_directory_entry_when_the_read_fails`는 현재 동작을 고정하는 가드라 통과함.

- [ ] **Step 3: 삽입 구현**

`src/state.rs`에서 `fn read_failure_message` 바로 위에 자유 함수 추가:

```rust
/// The `.` entry that heads a `DirsOnly` listing so the directory being browsed can itself be picked, including when it is empty.
fn current_dir_entry(current_dir: &Path) -> Entry {
    Entry {
        name: ".".to_string(),
        path: current_dir.to_path_buf(),
        kind: EntryKind::Directory,
        is_hidden: false,
        depth: 0,
    }
}
```

`refresh_entries`의 `Ok(entries)` 분기를 다음으로 교체:

```rust
        self.common.entries = match read {
            Ok(mut entries) => {
                self.common.read_error = None;
                if self.common.mode == PickerMode::DirsOnly {
                    entries.insert(0, current_dir_entry(&dir));
                }
                entries
            }
            Err(err) => {
                self.common.read_error = Some(read_failure_message(&err));
                Vec::new()
            }
        };
```

`build()`에서 `let (entries, read_error) = ...;` 블록을 다음으로 교체:

```rust
        let (mut entries, read_error) =
            match read_entries(&current_dir, self.show_hidden, self.filter.as_deref()) {
                Ok(entries) => (entries, None),
                Err(err) => (Vec::new(), Some(read_failure_message(&err))),
            };
        if read_error.is_none() && self.mode == PickerMode::DirsOnly {
            entries.insert(0, current_dir_entry(&current_dir));
        }
```

- [ ] **Step 4: 통과 확인**

Run: `cargo test --locked`
Expected: 전부 통과. 기존 테스트 중 기본 모드(`Both`)나 `FilesOnly`를 쓰는 것은 영향 없음. `files_only_mode_blocks_dir_selection` 같은 `DirsOnly`를 안 쓰는 테스트도 그대로.

- [ ] **Step 5: 문서**

`PickerMode::DirsOnly` 변형의 doc comment를 다음으로 교체:

```rust
    /// Only directories may be picked. `Enter` on a file does nothing.
    ///
    /// The listing starts with a `.` entry standing for the directory being browsed, so that directory can be picked itself, even when it is empty. `Enter` on it confirms; it cannot be entered or expanded.
    DirsOnly,
```

`CommonState::entries` 필드의 doc comment를 다음으로 교체:

```rust
    /// The rows to draw. In list view these are the entries of [`current_dir`](Self::current_dir); in tree view they are that directory plus every expanded descendant, flattened in display order. In [`PickerMode::DirsOnly`] the first entry is `.`, whose `path` is `current_dir` itself.
    pub entries: Vec<Entry>,
```

Run: `RUSTDOCFLAGS="-D warnings" cargo doc --locked --no-deps`
Expected: 경고 0.

- [ ] **Step 6: 커밋**

```bash
cargo fmt && cargo fmt --check
git add src/state.rs
git commit -m "feat: list the current directory as . in DirsOnly mode

A choose-folder dialog had no way to pick the folder the user had navigated into, and an empty folder could not be picked at all. DirsOnly listings now start with a . entry whose path is current_dir. It is inserted after a successful read, in refresh_entries and build, so an unreadable directory is never offered.

Claude-Session: https://claude.ai/code/session_01NowgbVqobryP8Qh2mN62Vz"
```

---

### Task 2: `.` 엔트리는 고를 수만 있고 들어가거나 펼칠 수 없다

**Files:**
- Modify: `src/state.rs` (`confirm` 430행 부근, `enter_directory` 483행 부근, `set_expanded_current` 279행 부근, tests 모듈)
- Modify: `tests/integration.rs` (파일 끝)

**Interfaces:**
- Consumes: Task 1의 `.` 엔트리 (`entry.path == self.common.current_dir`).

- [ ] **Step 1: 실패하는 단위 테스트 5개 추가**

`src/state.rs` tests 모듈, Task 1의 테스트들 뒤에:

```rust
    #[test]
    fn confirming_the_current_directory_entry_returns_the_current_directory() {
        let dir = make_dir_with_files();
        let mut state = FilePickerState::builder()
            .start_dir(dir.path())
            .mode(PickerMode::DirsOnly)
            .build();
        assert_eq!(state.current_entry().unwrap().name, ".");

        state.confirm();

        assert_eq!(
            state.result(),
            PickerResult::Selected(vec![state.common.current_dir.clone()])
        );
    }

    #[test]
    fn confirming_the_current_directory_entry_works_in_an_empty_directory() {
        let dir = TempDir::new().unwrap();
        let mut state = FilePickerState::builder()
            .start_dir(dir.path())
            .mode(PickerMode::DirsOnly)
            .build();

        state.confirm();

        assert_eq!(
            state.result(),
            PickerResult::Selected(vec![state.common.current_dir.clone()])
        );
    }

    #[test]
    fn the_current_directory_entry_can_be_multi_selected_across_directories() {
        let (_tmp, root) = make_tree_dir();
        let mut state = FilePickerState::builder()
            .start_dir(&root)
            .mode(PickerMode::DirsOnly)
            .build();

        state.toggle_select(); // "." in root
        *state.view.cursor_mut() = 1; // a_dir
        state.enter_directory();
        assert_eq!(state.current_entry().unwrap().name, ".");
        state.toggle_select(); // "." in a_dir
        state.confirm();

        assert_eq!(
            state.result(),
            PickerResult::Selected(vec![root.clone(), root.join("a_dir")])
        );
    }

    #[test]
    fn the_current_directory_entry_cannot_be_entered() {
        let dir = make_dir_with_files();
        let mut state = FilePickerState::builder()
            .start_dir(dir.path())
            .mode(PickerMode::DirsOnly)
            .build();
        let before = state.common.current_dir.clone();
        let count = state.common.entries.len();
        // Entering a directory resets the scroll offset; a real no-op leaves it alone.
        *state.view.scroll_offset_mut() = 1;

        state.enter_directory();
        state.descend();

        assert_eq!(state.common.current_dir, before);
        assert_eq!(state.common.entries.len(), count);
        assert_eq!(state.current_entry().unwrap().name, ".");
        assert_eq!(state.view.scroll_offset(), 1, "enter_directory must not run at all on .");
    }

    #[test]
    fn the_current_directory_entry_cannot_be_expanded() {
        let (_tmp, root) = make_tree_dir();
        let mut state = FilePickerState::builder()
            .start_dir(&root)
            .mode(PickerMode::DirsOnly)
            .view(ViewMode::Tree)
            .build();
        let count = state.common.entries.len();

        state.expand_current();
        state.toggle_expand_current();
        state.descend();

        assert_eq!(
            state.common.entries.len(),
            count,
            "expanding . would list the root twice"
        );
        let ViewState::Tree(tree) = &state.view else {
            panic!("tree view expected");
        };
        assert!(!tree.is_expanded(&state.common.current_dir));
    }

    #[test]
    fn entering_a_directory_puts_the_cursor_on_its_current_directory_entry() {
        let dir = make_dir_with_files();
        let mut state = FilePickerState::builder()
            .start_dir(dir.path())
            .mode(PickerMode::DirsOnly)
            .build();
        *state.view.cursor_mut() = 1; // subdir

        state.enter_directory();

        assert!(state.common.current_dir.ends_with("subdir"));
        assert_eq!(state.current_entry().unwrap().name, ".");
    }
```

- [ ] **Step 2: 실패 확인**

Run: `cargo test --locked --lib "current_directory_entry"`
Expected: `confirming_the_current_directory_entry_returns_the_current_directory`, `confirming_the_current_directory_entry_works_in_an_empty_directory`, `the_current_directory_entry_cannot_be_entered`, `the_current_directory_entry_cannot_be_expanded` FAILED. `confirm`은 폴더에서 진입하므로 `result()`가 `Pending`이고, `enter_directory`는 `current_dir`로 다시 진입하면서 스크롤 오프셋을 0으로 되돌리며, `expand_current`는 `current_dir`를 `expanded`에 넣어 목록이 두 배가 됨. `the_current_directory_entry_can_be_multi_selected_across_directories`와 `entering_a_directory_puts_the_cursor_on_its_current_directory_entry`는 Task 1만으로 통과하는 가드다.

- [ ] **Step 3: 가드 구현**

`confirm()`의 `match self.current_entry()` 첫 arm 앞에 arm 추가:

```rust
        match self.current_entry() {
            // The `.` entry stands for the directory being browsed: it is picked, never entered.
            Some(entry)
                if entry.kind == EntryKind::Directory
                    && entry.path == self.common.current_dir =>
            {
                let path = entry.path.clone();
                self.common.result = PickerResult::Selected(vec![path]);
            }
            Some(entry) if entry.kind == EntryKind::Directory => {
```

`enter_directory()`에서 `if entry.kind == EntryKind::File { return; }` 바로 뒤에:

```rust
        // The `.` entry already is the current directory.
        if entry.path == self.common.current_dir {
            return;
        }
```

`set_expanded_current()`에서 `if entry.kind != EntryKind::Directory { return; }` 바로 뒤에:

```rust
        // Expanding the `.` entry would list the current directory inside itself.
        if entry.path == self.common.current_dir {
            return;
        }
```

- [ ] **Step 4: 통과 확인**

Run: `cargo test --locked`
Expected: 전부 통과.

- [ ] **Step 5: 통합 테스트 추가 (키 입력 흐름)**

`tests/integration.rs` 파일 끝에:

```rust

/// Choose-folder dialog: walk into a folder, then Enter on `.` returns that folder.
#[test]
fn choose_folder_returns_the_directory_the_user_walked_into() {
    let tmp = setup_test_dir();
    let mut state = FilePickerState::builder()
        .start_dir(tmp.path())
        .mode(PickerMode::DirsOnly)
        .build();

    // Entries: ., src/, tests/, Cargo.toml, README.md
    state.handle_event(key(KeyCode::Down));
    assert_eq!(state.current_entry().unwrap().name, "src");
    state.handle_event(key(KeyCode::Enter)); // enter src/
    assert_eq!(state.current_entry().unwrap().name, ".");
    state.handle_event(key(KeyCode::Enter)); // pick "."

    match state.result() {
        PickerResult::Selected(paths) => {
            assert_eq!(paths.len(), 1);
            assert!(paths[0].ends_with("src"), "got {:?}", paths[0]);
        }
        other => panic!("expected Selected, got {:?}", other),
    }
}
```

Run: `cargo test --locked --test integration choose_folder`
Expected: PASS (Task 1과 Step 3의 결과를 키 경로로 확인하는 테스트. 실패하면 `handle_event`의 `Enter` 매핑이 `confirm()`이 아닌 것이므로 `src/event.rs`를 확인).

- [ ] **Step 6: 커밋**

```bash
cargo fmt && cargo fmt --check
git add src/state.rs tests/integration.rs
git commit -m "feat: confirm on . returns the current directory

Enter on the . entry selects current_dir instead of entering it, Space marks it like any other directory, and enter_directory and expansion ignore it. Expanding it would have listed the root inside itself in tree view.

Claude-Session: https://claude.ai/code/session_01NowgbVqobryP8Qh2mN62Vz"
```

---

### Task 3: 경로 바 왼쪽 잘라내기

**Files:**
- Modify: `src/widget.rs` (`render_path_bar` 71-76행, tests 모듈)

**Interfaces:**
- Produces: `fn truncate_path_left(path: &str, width: usize) -> String` (widget.rs 내부 자유 함수).

- [ ] **Step 1: 함수 단위 테스트 추가 (실패)**

`src/widget.rs` tests 모듈, `fn renders_without_panic()` 앞에:

```rust
    #[test]
    fn short_path_is_left_alone() {
        assert_eq!(truncate_path_left("/a/b", 10), "/a/b");
        assert_eq!(truncate_path_left("/a/b", 4), "/a/b");
    }

    #[test]
    fn long_path_keeps_its_tail_and_cuts_at_a_component() {
        let sep = std::path::MAIN_SEPARATOR;
        let path = ["", "Users", "namilkim", "Library", "Application Support", "app"]
            .join(&sep.to_string());
        let got = truncate_path_left(&path, 30);
        assert_eq!(got, format!("…{sep}Application Support{sep}app"));
        assert!(Span::raw(&got).width() <= 30);
    }

    #[test]
    fn last_component_wider_than_the_bar_is_cut_from_the_front() {
        let sep = std::path::MAIN_SEPARATOR;
        let path = ["", "x", "abcdefghijklmnop"].join(&sep.to_string());
        assert_eq!(truncate_path_left(&path, 8), "…jklmnop");
    }

    #[test]
    fn wide_characters_are_measured_by_columns() {
        let sep = std::path::MAIN_SEPARATOR;
        let path = ["", "홈", "문서", "프로젝트"].join(&sep.to_string());
        // "프로젝트" is 8 columns; with the separator and the ellipsis that is 10.
        let got = truncate_path_left(&path, 10);
        assert_eq!(got, format!("…{sep}프로젝트"));
        assert_eq!(Span::raw(&got).width(), 10);
    }

    #[test]
    fn bar_of_one_column_shows_only_the_ellipsis() {
        assert_eq!(truncate_path_left("/a/b", 1), "…");
    }
```

- [ ] **Step 2: 실패 확인**

Run: `cargo test --locked --lib truncate_path`
Expected: 컴파일 에러 `cannot find function truncate_path_left`.

- [ ] **Step 3: 함수 구현**

`src/widget.rs`의 `fn render_path_bar` 바로 위에:

```rust
/// Shortens a path for a bar `width` columns wide by dropping leading components, so the directory the user is actually in stays visible. A lone last component that is still too wide is cut from the front.
fn truncate_path_left(path: &str, width: usize) -> String {
    let display_width = |s: &str| Span::raw(s).width();
    if display_width(path) <= width {
        return path.to_string();
    }
    const ELLIPSIS: &str = "…";
    if width <= 1 {
        return ELLIPSIS.to_string();
    }
    let sep = std::path::MAIN_SEPARATOR.to_string();
    let parts: Vec<&str> = path.split(std::path::MAIN_SEPARATOR).collect();
    for start in 1..parts.len() {
        let candidate = format!("{ELLIPSIS}{sep}{}", parts[start..].join(&sep));
        if display_width(&candidate) <= width {
            return candidate;
        }
    }
    let last = parts.last().copied().unwrap_or("");
    let mut chars: Vec<char> = last.chars().collect();
    loop {
        let candidate = format!("{ELLIPSIS}{}", chars.iter().collect::<String>());
        if display_width(&candidate) <= width || chars.is_empty() {
            return candidate;
        }
        chars.remove(0);
    }
}
```

- [ ] **Step 4: 통과 확인**

Run: `cargo test --locked --lib truncate_path`
Expected: 5개 PASS.

- [ ] **Step 5: 렌더 테스트 추가 (실패)**

같은 tests 모듈에:

```rust
    #[test]
    fn path_bar_keeps_the_directory_name_when_the_panel_is_narrow() {
        let dir = TempDir::new().unwrap();
        let deep = dir.path().join("a-rather-long-directory-name").join("target");
        fs::create_dir_all(&deep).unwrap();
        let mut state = FilePickerState::builder().start_dir(&deep).build();

        let backend = TestBackend::new(20, 4);
        let mut terminal = Terminal::new(backend).unwrap();
        terminal
            .draw(|frame| {
                frame.render_stateful_widget(FilePicker::default(), frame.area(), &mut state);
            })
            .unwrap();

        let bar = row_text(&terminal, 0);
        assert!(bar.starts_with('…'), "got {bar:?}");
        assert!(bar.ends_with("target"), "got {bar:?}");
    }
```

Run: `cargo test --locked --lib path_bar_keeps`
Expected: FAILED. 현재는 오른쪽이 잘려 `bar`가 `/`로 시작하고 `target`으로 끝나지 않음.

- [ ] **Step 6: render_path_bar 교체**

```rust
fn render_path_bar(area: Rect, buf: &mut Buffer, state: &FilePickerState) {
    let path_str = state.common.current_dir.to_string_lossy();
    let shown = truncate_path_left(&path_str, area.width as usize);
    let style = state.common.theme.path_bar;
    let para = Paragraph::new(Line::from(Span::styled(shown, style)));
    para.render(area, buf);
}
```

- [ ] **Step 7: 통과 확인**

Run: `cargo test --locked`
Expected: 전부 통과.

- [ ] **Step 8: 커밋**

```bash
cargo fmt && cargo fmt --check
git add src/widget.rs
git commit -m "fix: truncate the path bar from the left

A 50-column panel showing a home directory path lost the one component that matters, the directory the user is in. The bar now drops leading components behind an ellipsis and only cuts into the last component when that alone does not fit. Width is measured in columns via Span::width, so wide characters do not overflow.

Claude-Session: https://claude.ai/code/session_01NowgbVqobryP8Qh2mN62Vz"
```

---

### Task 4: 상태 바에 `커서/전체` 위치 표시

**Files:**
- Modify: `src/widget.rs` (`render_status_bar`의 `InputMode::Normal` 분기, tests 모듈)
- Modify: `README.md` 12-20행과 140-153행의 목업 두 개

- [ ] **Step 1: 실패하는 렌더 테스트 2개 추가**

`src/widget.rs` tests 모듈에:

```rust
    #[test]
    fn status_bar_starts_with_the_cursor_position() {
        let dir = make_dir_with_n_files(5);
        let mut state = FilePickerState::builder().start_dir(dir.path()).build();
        *state.view.cursor_mut() = 2;

        let backend = TestBackend::new(60, 8);
        let mut terminal = Terminal::new(backend).unwrap();
        terminal
            .draw(|frame| {
                frame.render_stateful_widget(FilePicker::default(), frame.area(), &mut state);
            })
            .unwrap();

        assert_eq!(
            row_text(&terminal, 7),
            "3/5 | 0 selected | hidden: off | view: list"
        );
    }

    #[test]
    fn status_bar_shows_zero_of_zero_for_an_empty_listing() {
        let dir = TempDir::new().unwrap();
        let mut state = FilePickerState::builder()
            .start_dir(dir.path())
            .mode(PickerMode::FilesOnly)
            .build();

        let backend = TestBackend::new(60, 4);
        let mut terminal = Terminal::new(backend).unwrap();
        terminal
            .draw(|frame| {
                frame.render_stateful_widget(FilePicker::default(), frame.area(), &mut state);
            })
            .unwrap();

        assert!(
            row_text(&terminal, 3).starts_with("0/0 | "),
            "got {:?}",
            row_text(&terminal, 3)
        );
    }
```

`PickerMode`가 tests 모듈에 import되어 있지 않으면 `use crate::state::PickerMode;`를 테스트 안에 추가한다 (`tree_view_renders_indent_and_markers`가 `use crate::state::ViewMode;`를 함수 안에서 하는 것과 같은 방식).

- [ ] **Step 2: 실패 확인**

Run: `cargo test --locked --lib status_bar_`
Expected: 새 테스트 2개 FAILED (`0 selected | ...`로 시작). 기존 `status_bar_shows_read_error_while_directory_is_unreadable`는 통과.

- [ ] **Step 3: 구현**

`render_status_bar`의 `InputMode::Normal` 분기에서 `let text = format!(...)`를 다음으로 교체:

```rust
            let count = state.visible_count();
            let position = if count == 0 {
                0
            } else {
                state.view.cursor() + 1
            };
            let text = format!(
                "{}/{} | {} selected | hidden: {} | view: {}",
                position, count, selected_count, hidden_str, view_str
            );
```

- [ ] **Step 4: 통과 확인**

Run: `cargo test --locked`
Expected: 전부 통과. 상태 바 텍스트를 단언하는 다른 테스트는 없음 (`transient_error_message_takes_priority_over_read_error`는 에러 경로).

- [ ] **Step 5: README 목업 갱신**

상태 바가 43자로 길어져 기존 41칸 박스에 안 들어가므로 두 목업을 45칸 박스로 다시 그린다. `README.md` 12-20행의 리스트 목업을 다음으로 교체:

```
┌─────────────────────────────────────────────┐
│/home/you/projects/ratatree                  │
│   src/                                      │
│   tests/                                    │
│ * Cargo.toml                                │
│   config ->                                 │
│                                             │
│3/4 | 1 selected | hidden: off | view: list  │
└─────────────────────────────────────────────┘
```

`README.md` 140-153행의 트리 목업을 다음으로 교체:

```
┌─────────────────────────────────────────────┐
│/home/you/projects/ratatree                  │
│   ▾ src/                                    │
│     ▾ view/                                 │
│         list.rs                             │
│         mod.rs                              │
│       entry.rs                              │
│       state.rs                              │
│   ▸ tests/                                  │
│     Cargo.toml                              │
│     config ->                               │
│1/9 | 0 selected | hidden: off | view: tree  │
└─────────────────────────────────────────────┘
```

교체 후 `grep -n "selected | hidden" README.md`로 두 줄만 나오는지, 각 줄이 `│`로 시작하고 끝나는지 확인한다. 항목 행의 접두사 3칸, 들여쓰기, 마커는 0.2.0에서 실제 버퍼 덤프로 만든 것을 그대로 옮긴 것이고 이번에 바뀐 것은 상태 바 한 줄과 박스 폭뿐이다.

- [ ] **Step 6: 커밋**

```bash
cargo fmt && cargo fmt --check
git add src/widget.rs README.md
git commit -m "feat: show the cursor position in the status bar

A home directory with a few hundred entries gave no sense of where the cursor was. The status bar now starts with cursor/total. The README mockups are widened to fit the longer line.

Claude-Session: https://claude.ai/code/session_01NowgbVqobryP8Qh2mN62Vz"
```

---

### Task 5: 예제 `dialogs.rs`

**Files:**
- Create: `examples/dialogs.rs`
- Modify: `Cargo.toml` (`[[example]]` 블록 29-31행 뒤)
- Modify: `README.md` "Running the Example" 절 (239-243행)

**Interfaces:**
- Consumes: `FilePickerState::builder().start_dir().mode().filter().build()`, `FilePicker::default().block(Block<'static>)`, `PickerResult`, `ratatree::crossterm`. `dirs`는 ratatree의 일반 의존성이라 예제에서 쓸 수 있다.

- [ ] **Step 1: 예제 파일 작성**

`examples/dialogs.rs`:

```rust
//! The three dialogs an application usually needs, each shown as a modal over a small host screen.
//!
//! `1` opens a file, `2` chooses a folder, `3` picks several files. The modal is 50x18 on purpose: that is the size a real application gave the picker, and it is where a long path and a big directory still have to make sense.

use std::io;
use std::path::PathBuf;

use ratatui::backend::CrosstermBackend;
use ratatui::layout::{Constraint, Flex, Layout, Rect};
use ratatui::style::{Style, Stylize};
use ratatui::text::{Line, Span};
use ratatui::widgets::{Block, Clear, Paragraph};
use ratatui::{Frame, Terminal};

use ratatree::crossterm::event::{self, Event, KeyCode, KeyEventKind};
use ratatree::crossterm::execute;
use ratatree::crossterm::terminal::{
    disable_raw_mode, enable_raw_mode, EnterAlternateScreen, LeaveAlternateScreen,
};
use ratatree::{FilePicker, FilePickerState, PickerMode, PickerResult};

/// Which dialog is open, so the result can be labelled and the picker built to match.
#[derive(Clone, Copy)]
enum Dialog {
    OpenFile,
    ChooseFolder,
    PickFiles,
}

impl Dialog {
    fn title(self) -> &'static str {
        match self {
            Dialog::OpenFile => " Open file ",
            Dialog::ChooseFolder => " Choose folder ",
            Dialog::PickFiles => " Pick files ",
        }
    }

    fn build(self) -> FilePickerState {
        let start = dirs::home_dir().unwrap_or_else(|| PathBuf::from("."));
        let builder = FilePickerState::builder().start_dir(start);
        match self {
            Dialog::OpenFile => builder
                .mode(PickerMode::FilesOnly)
                .filter(|path| {
                    path.extension()
                        .is_some_and(|ext| ext == "pdf" || ext == "md")
                })
                .build(),
            Dialog::ChooseFolder => builder.mode(PickerMode::DirsOnly).build(),
            Dialog::PickFiles => builder.mode(PickerMode::Both).build(),
        }
    }
}

struct App {
    picker: Option<(Dialog, FilePickerState)>,
    last: String,
}

fn main() -> io::Result<()> {
    enable_raw_mode()?;
    let mut stdout = io::stdout();
    execute!(stdout, EnterAlternateScreen)?;
    let mut terminal = Terminal::new(CrosstermBackend::new(stdout))?;

    let mut app = App {
        picker: None,
        last: "Nothing picked yet.".to_string(),
    };
    let result = run(&mut terminal, &mut app);

    disable_raw_mode()?;
    execute!(terminal.backend_mut(), LeaveAlternateScreen)?;
    terminal.show_cursor()?;
    result
}

fn run(terminal: &mut Terminal<CrosstermBackend<io::Stdout>>, app: &mut App) -> io::Result<()> {
    loop {
        terminal.draw(|frame| draw(frame, app))?;

        let Event::Key(key) = event::read()? else {
            continue;
        };
        if key.kind == KeyEventKind::Release {
            continue;
        }

        // While a dialog is open every key goes to it. Its result decides when it closes.
        if let Some((dialog, picker)) = &mut app.picker {
            picker.handle_event(Event::Key(key));
            let label = dialog.title().trim();
            let outcome = match picker.result() {
                PickerResult::Selected(paths) => {
                    let list: Vec<String> = paths.iter().map(|p| p.display().to_string()).collect();
                    Some(format!("{label}: {}", list.join(", ")))
                }
                PickerResult::Cancelled => Some(format!("{label}: cancelled")),
                PickerResult::Pending => None,
            };
            if let Some(text) = outcome {
                app.last = text;
                app.picker = None;
            }
            continue;
        }

        match key.code {
            KeyCode::Char('1') => app.picker = Some((Dialog::OpenFile, Dialog::OpenFile.build())),
            KeyCode::Char('2') => {
                app.picker = Some((Dialog::ChooseFolder, Dialog::ChooseFolder.build()))
            }
            KeyCode::Char('3') => app.picker = Some((Dialog::PickFiles, Dialog::PickFiles.build())),
            KeyCode::Char('q') | KeyCode::Esc => return Ok(()),
            _ => {}
        }
    }
}

fn draw(frame: &mut Frame, app: &mut App) {
    let area = frame.area();
    let help = Line::from(vec![
        Span::styled("1", Style::new().bold()),
        Span::raw(": open file   "),
        Span::styled("2", Style::new().bold()),
        Span::raw(": choose folder   "),
        Span::styled("3", Style::new().bold()),
        Span::raw(": pick files   "),
        Span::styled("q", Style::new().bold()),
        Span::raw(": quit"),
    ]);
    let body = vec![help, Line::raw(""), Line::raw(app.last.as_str())];
    frame.render_widget(
        Paragraph::new(body).block(Block::bordered().title(" dialogs ")),
        area,
    );

    if let Some((dialog, picker)) = &mut app.picker {
        let modal = centered(area, 50, 18);
        frame.render_widget(Clear, modal);
        let widget = FilePicker::default().block(Block::bordered().title(dialog.title()));
        frame.render_stateful_widget(widget, modal, picker);
    }
}

fn centered(area: Rect, width: u16, height: u16) -> Rect {
    let [area] = Layout::horizontal([Constraint::Length(width)])
        .flex(Flex::Center)
        .areas(area);
    let [area] = Layout::vertical([Constraint::Length(height)])
        .flex(Flex::Center)
        .areas(area);
    area
}
```

- [ ] **Step 2: Cargo.toml에 예제 등록**

`Cargo.toml`의 기존 `[[example]]` 블록 뒤에:

```toml

[[example]]
name = "dialogs"
path = "examples/dialogs.rs"
```

- [ ] **Step 3: 컴파일과 린트 확인**

Run: `cargo build --locked --example dialogs && cargo clippy --locked --all-targets -- -D warnings`
Expected: 경고 0, 에러 0. 흔한 실패: `dirs` 미해결이면 `Cargo.toml [dependencies]`에 `dirs = "7"`가 있는지 확인(있어야 정상). `Block<'static>` 수명 에러면 `title()`이 `&'static str`을 돌려주는지 확인.

- [ ] **Step 4: 실제 터미널에서 한 번 돌려보기**

Run: `cargo run --locked --example dialogs`
확인: `2`를 눌러 폴더 고르기 모달이 뜨고 목록 첫 줄이 `./`인지, `Enter`로 홈 폴더 경로가 호스트 화면 `last`에 찍히는지, `1`에서 `.md`/`.pdf`만 보이는지, 경로 바가 `…`로 시작하며 마지막 폴더 이름이 보이는지(홈에서 `Library` → `Application Support`로 들어가면 됨), 상태 바가 `N/M`으로 시작하는지. `q`로 종료. 이 단계는 자동화되지 않으므로 결과를 커밋 메시지가 아니라 세션 로그에 적는다.

- [ ] **Step 5: README 갱신**

`README.md`의 "Running the Example" 절을 다음으로 교체:

```markdown
## Running the Examples

```bash
cargo run --example basic     # the picker filling the whole terminal
cargo run --example dialogs   # open-file, choose-folder and pick-files modals over a host screen
```
```

- [ ] **Step 6: 커밋**

```bash
cargo fmt && cargo fmt --check
git add examples/dialogs.rs Cargo.toml README.md
git commit -m "docs: add the dialogs example

Three modals over a host screen: open a file with an extension filter, choose a folder through the . entry, pick several files with Space. The modal is 50x18, the size a dependent application uses, so the narrow-panel behaviour is what the example exercises.

Claude-Session: https://claude.ai/code/session_01NowgbVqobryP8Qh2mN62Vz"
```

---

### Task 6: 0.4.0 릴리스 준비 (배포 제외)

**Files:**
- Modify: `Cargo.toml` (3행 `version`), `Cargo.lock`
- Modify: `CHANGELOG.md` (7행 `## [0.3.0]` 앞)
- Modify: `README.md` (31행 `ratatree = "0.3"`, 74-83행 Features, 118-127행 Actions 표)

- [ ] **Step 1: 버전 범프**

`Cargo.toml` 3행을 `version = "0.4.0"`으로 바꾸고:

```bash
cargo update --workspace --offline
git diff --stat Cargo.lock   # 1 file changed, 1 insertion(+), 1 deletion(-) 이어야 함
```

- [ ] **Step 2: CHANGELOG**

`CHANGELOG.md`의 `## [0.3.0] - 2026-09-12` 바로 앞에:

```markdown
## [0.4.0] - unreleased

The three dialogs an application usually needs, open a file, choose a folder and pick several files, now work without help in a narrow panel, a long path, a large directory and an empty folder.

### Migration

- In `PickerMode::DirsOnly` the first entry of `CommonState::entries` is now `.`, whose `path` is `current_dir`. Code that iterates `entries` or indexes `entries[0]` in that mode sees it. `FilesOnly` and `Both` are unchanged.
- `PickerResult::Selected` can now return `current_dir` itself in `DirsOnly` mode.

### Added

- `.` entry in `DirsOnly` mode. A choose-folder dialog had no way to pick the folder the user had navigated into, and an empty folder could not be picked at all. `Enter` or `Space` on `.` picks the current directory; it cannot be entered or expanded.
- Cursor position in the status bar: `3/340 | 0 selected | ...`. A home directory with a few hundred entries gave no sense of where the cursor was.
- `examples/dialogs.rs`: the three dialogs as 50x18 modals over a host screen.

### Changed

- The path bar truncates from the left. A 50-column panel showing a home directory path used to lose the one component that matters, the directory the user is in. It now reads `…/Application Support/app`.
```

- [ ] **Step 3: README**

31행을 `ratatree = "0.4"`로. Features 목록의 `- **Multi-select**` 줄 뒤에:

```markdown
- **Choose-folder dialogs** - in `DirsOnly` mode the listing starts with `.`, so the folder being browsed can be picked itself, even when it is empty
```

Actions 표의 `Enter` 행을 다음으로 교체:

```markdown
| `Enter` | Confirm selection (or enter directory; tree view: expand/collapse it). On the `.` entry in `DirsOnly` mode, confirm the current directory |
```

- [ ] **Step 4: 전체 검증**

```bash
cargo fmt --check
cargo test --locked
cargo +1.88.0 test --locked
cargo clippy --locked --all-targets -- -D warnings
RUSTDOCFLAGS="-D warnings" cargo doc --locked --no-deps
cargo package --locked --allow-dirty
```

Expected: fmt 출력 없음. 테스트 3개 바이너리 전부 `0 failed` (0.3.0 기준 112개에 이번 계획으로 약 20개 추가). clippy와 doc `Finished`만. package `Packaged N files` 뒤 `Verifying`/`Compiling`/`Finished`. 하나라도 실패하면 이 태스크에서 고치고 다시 돌린다.

- [ ] **Step 5: 커밋**

```bash
git add Cargo.toml Cargo.lock CHANGELOG.md README.md
git commit -m "chore: bump version to 0.4.0 and write the changelog

Claude-Session: https://claude.ai/code/session_01NowgbVqobryP8Qh2mN62Vz"
```

push, CI 확인, CHANGELOG 날짜 확정, `cargo publish`, 태그는 이 계획 밖이다. 사용자 확인을 받고 진행한다.

---

## Self-Review

**Spec coverage:**
- 1. `.` 항목: 삽입/모드/실패 시 미삽입/트리 루트만 (Task 1), confirm/Space/진입·펼침 차단/진입 후 커서 (Task 2), 문서 (Task 1 Step 5, Task 6 Step 3). 렌더링은 기존 `Directory` 스타일과 `/` 접미가 그대로 적용되므로 별도 작업 없음. `toggle_view`/`reset`의 경로 유지는 `.`의 path가 `current_dir`라 그대로 동작하며, `refresh_entries`가 `.`을 다시 넣으므로 `move_cursor_to_path`가 찾는다.
- 2. 경로 바: Task 3. 폭 1 이하, 컴포넌트 경계, 마지막 컴포넌트 문자 단위, wide char 전부 테스트.
- 3. 상태 바: Task 4. `0/0`, 검색 모드 미변경.
- 4. 예제: Task 5. 50x18, `Clear`, 세 모드, 필터, 제목.
- 5. 릴리스: Task 6. 배포 제외.
- 제외 항목은 어느 태스크에도 없음.

**Placeholder scan:** 없음. 모든 코드 단계에 실제 코드가 있다.

**Type consistency:** `current_dir_entry(&Path) -> Entry` (Task 1)와 Task 2의 판별식 `entry.path == self.common.current_dir` 일치. `truncate_path_left(&str, usize) -> String` (Task 3) 시그니처가 테스트와 구현에서 같음. Task 4 테스트의 기대 문자열 `"3/5 | 0 selected | hidden: off | view: list"`가 구현 format과 같음. Task 5의 `Dialog::title() -> &'static str`이 `Block<'static>` 요구와 맞음.
